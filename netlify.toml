[build]
  publish = "dist"
  command = "npm run build"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

# Copy images directory to dist for proper paths
[[plugins]]
  package = "@netlify/plugin-build-directory"

[build.environment]
  NODE_VERSION = "18"
